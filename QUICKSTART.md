# 快速开始指南

## 🚀 一键启动（支持热重载）

```bash
# 安装依赖（仅首次需要）
npm i docsify-cli -g

# 启动开发服务器（自动监听文件变化）
npm run serve
```

启动后：
- ✅ 自动打开浏览器（http://localhost:3000）
- ✅ 自动监听所有 `.md` 文件变化
- ✅ 保存文件后自动刷新浏览器
- ✅ 无需手动操作

## 📝 实时编辑工作流

1. **启动服务器**
   ```bash
   npm run serve
   ```

2. **编辑文档**
   - 打开任何 `.md` 文件进行编辑
   - 例如：编辑 `README.md` 或 `analysis_2026/huize/慧择_价值投资分析报告_2026-01-23.md`

3. **保存文件**
   - 按 `Cmd+S` (Mac) 或 `Ctrl+S` (Windows/Linux) 保存
   - 浏览器**自动刷新**，立即看到更新

4. **继续编辑**
   - 无需任何额外操作
   - 每次保存都会自动刷新

## 🎯 常用命令

```bash
# 启动开发服务器（端口 3000，自动打开浏览器）
npm run serve

# 启动开发服务器（自定义端口）
docsify serve . --port 8080

# 启动开发服务器（不自动打开浏览器）
docsify serve . --port 3000

# 查看所有选项
docsify serve --help
```

## ⚡ 热重载特性（LiveReload）

docsify 内置了 **LiveReload** 功能，默认启用：

- **自动检测**：监听所有 Markdown 文件（`.md`）
- **即时刷新**：保存后 1-2 秒内自动刷新浏览器
- **无需重启**：服务器持续运行，无需手动操作
- **跨文件**：修改任何文件都会触发刷新
- **默认端口**：LiveReload 使用端口 35729（自动配置）

**工作原理**：
- docsify 使用 LiveReload 技术
- 服务器监听文件系统变化
- 通过 WebSocket 向浏览器发送刷新信号
- 浏览器自动重新加载页面

## 🔧 故障排除

**问题：保存后浏览器没有自动刷新**

解决方案：
1. 确保服务器正在运行（终端窗口保持打开）
2. 检查浏览器控制台是否有错误
3. 尝试手动刷新浏览器（`Cmd+R` 或 `Ctrl+R`）
4. 重启服务器：按 `Ctrl+C` 停止，然后重新运行 `npm run serve`

**问题：端口被占用**

解决方案：
```bash
# 使用其他端口
docsify serve . --port 8080
```

**问题：修改 index.html 后没有变化**

解决方案：
- `index.html` 的修改可能需要手动刷新浏览器
- 或者重启服务器

## 📚 更多信息

查看 [DOCSIFY.md](DOCSIFY.md) 了解详细配置和高级功能。
